## Installation
```bash
# simple1
# sc free not prem & open source

# Perintah Install Script
$ apt update && apt upgrade -y
$ pkg install python
$ pkg install git
$ pip install requests
$ pip install bs4
$ pip install rich
$ pip install stdiomask
$ git clone https://github.com/Rfnn23/simple1
$ cd simple1
$ python rfnsimple1.py
```
